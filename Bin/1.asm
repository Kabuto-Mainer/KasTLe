;============================================
; THIS FILE IS AUTOGENERATED BY KasTLe
; DO NOT MODIFY IT
;============================================

section .data

section .rodata
; Constant Strings

section .text
global _start
_start:
    push rbp
    mov  rbp, rsp
    mov  rax, 10
    push rax
    pop  rdi
    call __func__fact
    mov  dword [rbp-16], eax
    mov  rax, 60
    xor  rdi, rdi
    syscall
global __func__fact
__func__fact:
    push rbp
    mov  rbp, rsp
    sub  rsp, 16
    mov  qword [rbp-8], rdi
    lea  rax, qword [rbp-8]
    mov  eax, dword [rax]
    push rax
    mov  rax, 1
    pop  rdi
    cmp  rdi, rax
    sete al
    movzx rax, al
    test rax, rax
    jz   .L0
    mov  rax, 1
    mov  rsp, rbp
    pop  rbp
    ret
    jmp  .L0
.L0:
.L0:
    lea  rax, qword [rbp-8]
    mov  eax, dword [rax]
    push rax
    lea  rax, qword [rbp-8]
    mov  eax, dword [rax]
    push rax
    mov  rax, 1
    pop  rdi
    sub  rdi, rax
    mov  rax, rdi
    push rax
    pop  rdi
    sub  rsp, 8
    call __func__fact
    add  rsp, 8
    pop  rdi
    imul rax, rdi
    mov  rsp, rbp
    pop  rbp
    ret
    mov  rsp, rbp
    pop  rbp
    ret
